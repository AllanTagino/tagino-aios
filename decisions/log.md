# Decisions log

> Decisões importantes do negócio. Append-only.
> Skill `/decidir` adiciona entries com a estrutura: contexto + decisão + por quê + impacto.
>
> **Não editar entries antigas.** Se uma decisão for revertida, adicionar nova entry mencionando "revoga decisão de YYYY-MM-DD" — a história importa pra entender por que algo mudou.
>
> Skills que leem esse arquivo: `/atualizar` (pra propor refresh de memória), `/audit` (pontuação), `/mapear-rotinas` (contexto). Skill `/abrir` opcionalmente menciona a decisão mais recente.

---

_(vazio — primeira decisão será adicionada via `/decidir`)_
